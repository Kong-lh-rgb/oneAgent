Durable Run Manager。这是下一个最重要的基础模块。现在 Run 还是偏“用户发一句话启动一次 Runtime”。以后应该变成真正的生命周期对象：start / pause / resume / cancel / retry / status / background。程序重启之后，正在执行或等待的 Run 也应该能恢复。Checkpoint 就从“有恢复数据”真正升级成“系统能恢复长期工作”。
Automation / Scheduler。支持“下午五点做”“每天早上做”“一小时后继续”“每隔一天检查一次”。进一步再支持 Condition Watch，比如“文件出现了再处理”“GitHub Issue 有变化再运行”“某个 Task 到时间继续”。这会是你和普通 Coding Agent 很明显的区别：Agent 不需要用户每次主动发消息才活过来。
Computer Runtime。如果最终定位真的是电脑端个人助手，这个绕不过去。现在你已经能读写文件、Shell、HTTP、搜索，但这仍然更接近 Coding Agent。 后面应该抽一个 ComputerRuntime：浏览器自动化、窗口/App 操作、键鼠、剪贴板、截图、系统通知，最好优先使用 Accessibility/UI Tree 这种结构化信息，截图坐标作为 fallback。先支持一个 OS 就够，不要一开始跨三平台。
Browser Automation。不要单独搞什么 Browser Agent，直接把浏览器当 Computer Runtime 的一个能力。Playwright 这种结构化浏览器控制非常实用：登录网站、下载文件、填表、查资料、处理后台页面。对于“个人电脑助手”来说，它甚至比 Multi-Agent 重要得多。
真正的 Sandbox + Security Boundary。你现在 Shell 已经有限定 workspace、timeout、Human Approval，但本质上还是本机 subprocess。 后面可以形成 read-only / workspace-write / full-access 这种明确权限档位，再控制文件路径、网络域名、进程、Secrets。Codex 现在把 sandbox + approval + network policy 当作很核心的 Harness 能力，这块如果你做好，对简历也非常有分量。
Agent Service / API。把 CLI 和 Agent Core 分开。以后应该是 Agent Server → RunManager → AgentRuntime，CLI 只是一个 client。这样 Desktop UI、CLI、Scheduler、甚至未来手机端都使用同一个后端。OpenHands 和 Goose 都走了类似的 client/server 方向。
Desktop App。这个不是现在立刻做，但 Computer Runtime + API 稳定后就非常值得做。桌面端不用复杂：左边 Conversation/Task，中间聊天和当前执行，右边显示 Runs、Approval、Artifacts。系统托盘再支持后台运行和通知。你的定位一旦是 personal desktop assistant，最终只有 CLI 会比较吃亏；Goose 已经明确同时提供 Desktop、CLI、API。
Async Human Approval。现在 approval 偏 Run 内即时确认。长期 Agent 应该支持：Agent 在后台运行 → 遇到危险操作暂停 → 桌面通知用户 → 用户稍后 Accept/Reject → Run 继续。这个和 Scheduler、Checkpoint、RunManager 串起来以后非常漂亮。
Artifact / Result System。不要让一次 Task 的结果只是聊天回复。报告、代码文件、下载文件、图片、CSV、网页都应该形成 Artifact，关联 task_id/run_id。这样 Desktop UI 里可以明确显示“Agent 这次替我产出了什么”。
Workspace / Project Profile。类似 Claude Code 的 CLAUDE.md 和 Codex 的 AGENTS.md 思路，给 workspace 保存项目级指令、环境信息、常用命令和权限策略。Claude Code 会跨会话读取项目和用户级 Memory，Codex 也支持通过项目文件提供仓库工作方式。 你的 Skill/Memory 已经比它更丰富，但一个简单明确的 project profile 还是很好用。
Proactive Personal Assistant。这是最后真正把你和 Coding Agent 拉开的核心：Agent 不只是“我叫它干活”，而是基于 Automation + Task + Memory 主动执行。例如每天整理下载目录、定时汇总信息、检查项目状态、根据用户长期习惯执行固定工作流。你的 Skill Learning 正好可以在这里发挥最大价值：使用越久，越来越知道用户反复怎么做事。
最后再考虑 Parallel Runs / Subagents。Codex App 已经把多个 Agent 并行和隔离 worktree 做成核心体验。 但你现在没必要直接上“Multi-Agent Swarm”。先让 RunManager 支持多个独立 Run 并发、项目隔离、统一监督，就已经很有价值了。之后真的遇到一个任务需要委派多个 Worker，再加 Subagent。